import { AnalyticsInterceptor } from './analytics.interceptor';

describe('AnalyticsInterceptor', () => {
  it('should be defined', () => {
    expect(new AnalyticsInterceptor()).toBeDefined();
  });
});
